# Relay 0.4.9 UI polish

Compact typography and identity headers; solid window backing; more vertical space for route selection; single-line buttons; metric card minimum height and increased inset. Unpowered hubs hide route editing, maintenance controls require management access, and route pagination is hidden when unnecessary. Draft status and the power banner are quieter.

Native interaction tests pass: CloseLifecycle, DesignAndSnapshots, NativeWidgetLifecycle. Layout coverage includes three viewport sizes, UI scales 1/1.5/2, powered/unpowered hubs and an assertion for hidden route editing.

Actual Slate screenshot capture was attempted, but the VM software Vulkan driver failed the engine feature-profile check before rendering. Visual acceptance remains a Windows in-game check; geometry tests are not screenshot validation.

The 0.4.8 opaque detail-material fix is retained. This release does not change transport, meshes, materials, or save formats.

Second pass: draft route statistics resolve against the current name/channel/medium; route rows show member counts; endpoint panels explain send/receive direction; directory filtering requires a powered hub and selected route; endpoint pagination appears only when needed. See [research and references](ui-reference-review.md). Final interaction suite: 2 successes, 1 success with SDK Blueprint warnings, 0 failures.
