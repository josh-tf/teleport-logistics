# Relay 0.5.5 reward lifetime correction

The supplied 0.5.4 crash is different from the previous Build Gun construction assertion. It reaches SML's `UModContentRegistry::AutoRegisterUnlockReferences` and fails with an access violation. The retained `Relay.LastConstructingClass` field records the last construction entry and does not identify the current crash site.

Deferred Blueprint reward objects introduced in 0.5.4 were referenced only by native schematic class-default objects. Native CDOs can reside in Unreal's disregard-for-GC set; adding runtime references there does not reliably keep newly allocated objects alive. Keep explicit `TStrongObjectPtr` references for the two replacement unlocks for the module lifetime. Their reflected recipe arrays, Blueprint class and presentation dependencies remain reachable. Repeated initialization reuses existing objects.

The content inspection now forces garbage collection after deferred creation and before reading the rewards, then checks the interfaces, preserved recipe counts and idempotency. This supplements the earlier editor checks; the editor does not fully reproduce the retail GC configuration.

Deferred loading from 0.5.4 is retained. Build Gun diagnostics are retained. No models, recipe costs, tiers or save identifiers change. Retail validation remains required.

Completed audit: see `0.5.5-research-audit.md`. Eight native tests passed, including removal of CDO GC edges; exact content inspection and transport/model validations passed. Final Windows Steam/Epic/server builds and cooks exited successfully (`/tmp/relay-055-package-final.log`). Retail verification is still pending.
